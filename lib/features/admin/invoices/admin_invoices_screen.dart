import 'package:flutter/material.dart';
import '../../user/invoices/invoices_screen.dart';

/// Admin melihat semua invoice (API dashboard/invoices filter by role di backend).
class AdminInvoicesScreen extends StatelessWidget {
  const AdminInvoicesScreen({super.key});

  @override
  Widget build(BuildContext context) => const InvoicesScreen();
}

<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <!-- padded so logo tidak terlalu besar / menempel edge -->
    <item
        android:left="48dp"
        android:top="48dp"
        android:right="48dp"
        android:bottom="48dp">
        <bitmap
            android:src="@drawable/mport_splash_logo"
            android:gravity="center"
            android:filter="true" />
    </item>
</layer-list>
